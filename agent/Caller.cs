using UnityEngine;

namespace AgentAPI {
    public class Caller : MonoBehaviour
    {
        public string newKeywords;

        private void Start()
        {
            // // Get a reference to the Agent component attached to this GameObject
            // UserAPI agent = GetComponent<UserAPI>();

            // // Set the userID property to a unique identifier for the current user
            // agent.userID = 1234; // Replace with your own user ID

            // // Set the newKeywords property to the desired value
            // agent.newKeywords = "new keywords";

            // // Send the updated data to the server
            // agent.SendUpdate();
        }
    }
}